# engine

Work arrives faster than one agent can carry it, so a foreman decomposes the job into self-contained tasks and delegates each to a worker.

## Getting started

```bash
uv sync
uv run engine-control-server
```
