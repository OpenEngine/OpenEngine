[project]
name = "engine-github-concierge"
version = "0.0.0"
description = "Pull-request conversation graph and its explicit egress boundary."
requires-python = ">=3.11"
dependencies = ["engine-domain", "engine-single-tool-mcp", "langgraph-acp", "langgraph>=1.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/engine"]

[tool.uv.sources]
engine-domain = { workspace = true }
engine-single-tool-mcp = { workspace = true }
langgraph-acp = { path = "../../langgraph-acp", editable = true }
