[project]
name = "engine-orchestrator"
version = "0.0.0"
description = "Owns Engine's first-class workflows and their Temporal lifecycle."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/engine"]
